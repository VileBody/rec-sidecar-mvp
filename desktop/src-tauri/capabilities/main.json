{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Least-privilege access for the bundled REC Coach main window",
  "windows": ["main"],
  "local": true,
  "permissions": [
    "core:event:default",
    "core:window:default",
    "allow-rec-coach-commands"
  ]
}
